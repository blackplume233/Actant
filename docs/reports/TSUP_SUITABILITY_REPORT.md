# tsup 适用性专项分析报告

## 执行摘要

| 评估维度 | 结论 | 风险等级 |
|----------|------|----------|
| 整体适用性 | **部分适用** | 🟡 中 |
| 库包 (shared/core) | ✅ 完全适合 | 🟢 低 |
| 应用包 (api/cli/mcp-server) | ❌ 不适合 | 🔴 高 |
| 长期可维护性 | ⚠️ 需要调整 | 🟡 中 |

**核心结论**: tsup 是优秀的库构建工具，但 AgentCraft 包含多个应用包，继续使用 tsup 会在发布和部署阶段产生问题。建议 **库保持 tsup，应用迁移到 tsc**。

---

## 1. 项目包分类分析

### 1.1 包类型识别

```
packages/
├── shared        📦 库 (Library)
│   └── 用途: 被其他包依赖，提供类型和工具
│
├── core          📦 库 (Library)
│   └── 用途: 被 api/cli/mcp-server 依赖
│
├── api           🚀 应用 (Application)
│   └── 用途: HTTP 服务，独立运行
│
├── cli           🚀 应用 (Application)
│   └── 用途: 命令行工具，独立运行
│
├── mcp-server    🚀 应用 (Application)
│   └── 用途: MCP 协议服务，独立运行
│
└── acp           📦/🚀 混合 (Library + Service)
    └── 用途: 可能被依赖，也可能独立运行
```

### 1.2 库 vs 应用的需求差异

| 需求维度 | 库 (Library) | 应用 (Application) |
|----------|--------------|-------------------|
| **产物类型** | ESM/CJS 均可，需类型声明 | 通常只需一种格式，无需类型声明 |
| **代码分割** | 有利于 tree-shaking | 可能增加启动延迟 |
| **入口文件** | 单一 index.ts | 多入口 (bin/、daemon-entry.ts) |
| **shebang** | 不需要 | CLI 必须 (#!/usr/bin/env node) |
| **依赖处理** | externalize（不打包依赖） | bundle 或 externalize 均可 |
| **体积敏感** | 是（影响安装速度） | 相对不敏感 |
| **调试需求** | sourcemap 足够 | 可能需要原始文件结构 |

---

## 2. tsup 在各包的表现分析

### 2.1 shared - ✅ 完全适合

```typescript
// tsup.config.ts
export default defineConfig({
  entry: ["src/index.ts"],
  format: ["esm"],
  dts: true,
});
```

| 评估项 | 表现 | 评价 |
|--------|------|------|
| 构建产物 | dist/index.js + index.d.ts | ✅ 符合库规范 |
| 类型声明 | 自动生成 | ✅ 必要 |
| 代码分割 | 不需要 | ✅ 单一入口 |
| 外部依赖 | pino 被 external | ✅ 正确 |
| 发布 npm | 可直接发布 | ✅ 标准库格式 |

**结论**: 完美契合，无需调整。

---

### 2.2 core - ✅ 适合

```typescript
// tsup.config.ts
export default defineConfig({
  entry: ["src/index.ts"],
  format: ["esm"],
  dts: true,
});
```

**测试通过情况**: ✅ 161 个测试全部通过

**潜在问题**:
- 模板 fixtures (JSON 文件) 需要复制到 dist
- 当前配置未处理静态资源

**改进建议**:
```typescript
export default defineConfig({
  entry: ["src/index.ts"],
  format: ["esm"],
  dts: true,
  // 添加资源复制
  esbuildOptions: (options) => {
    options.loader = {
      ...options.loader,
      ".json": "file",
    };
  },
});
```

---

### 2.3 cli - ❌ 不适合

#### 当前配置
```typescript
export default defineConfig({
  entry: ["src/index.ts", "src/bin/agentcraft.ts", "src/daemon-entry.ts"],
  format: ["esm"],
  dts: true,  // ❌ CLI 不需要类型声明
});
```

#### 构建产物分析
```
dist/
├── bin/                           # 需要手动复制？
├── chunk-XHQCPNMP.js             # ❌ 代码分割，CLI 不需要
├── daemon-entry.js               # ✅ 入口
├── daemon-entry.d.ts             # ❌ 无用
├── index.js                      # ✅ 入口
├── index.d.ts                    # ❌ 无用
└── ...
```

#### 具体问题

| 问题 | 影响 | 严重程度 |
|------|------|----------|
| **缺少 shebang** | `./dist/bin/agentcraft.js` 无法直接执行 | 🔴 高 |
| **代码分割** | 启动时需要加载多个 chunk，延迟增加 | 🟡 中 |
| **生成 .d.ts** | 产物臃肿，无意义 | 🟢 低 |
| **多入口管理** | 产物分散，需要手动处理 bin 链接 | 🟡 中 |

#### 验证 shebang 问题
```bash
# 构建后尝试直接运行
./packages/cli/dist/bin/agentcraft.js
# 结果: 失败，缺少 shebang，需要 node ./packages/cli/dist/bin/agentcraft.js
```

#### CLI 的特殊要求

```json
// package.json 期望
{
  "bin": {
    "agentcraft": "./dist/bin/agentcraft.js"
  }
}

// 期望的产物
#!/usr/bin/env node
// bundled code here...
```

**结论**: tsup 不是为 CLI 工具设计的，shebang 处理是硬伤。

---

### 2.4 api - ⚠️ 部分适合

#### 当前配置
```typescript
export default defineConfig({
  entry: ["src/index.ts"],
  format: ["esm"],
  dts: true,  // ❌ 服务不需要
});
```

#### 问题分析

| 问题 | 说明 | 影响 |
|------|------|------|
| **生成 .d.ts** | HTTP 服务不需要类型声明 | 产物体积 +30% |
| **sourcemap** | 生产环境通常不需要 | 部署包增大 |
| **__dirname 问题** | ESM 中 __dirname 不可用 | 可能运行时错误 |

#### 服务端代码潜在风险
```typescript
// 如果代码中使用 __dirname
const templatePath = path.join(__dirname, "../templates");
// ESM 打包后会报错: __dirname is not defined
```

**结论**: 能用，但产物不是最优，且存在 ESM 兼容性风险。

---

### 2.5 mcp-server - ⚠️ 同 api

与 api 包相同的问题，作为服务应用，tsup 构建产物不是最优。

---

## 3. tsup 的局限性详细分析

### 3.1 设计定位冲突

```
tsup 设计目标:
├─ 快速构建 TypeScript 库
├─ 生成可发布的 npm 包
├─ 支持 ESM/CJS 双模式
└─ 自动生成类型声明

AgentCraft 应用包需求:
├─ CLI: 可执行文件 + shebang
├─ API: 服务端运行 + 无类型声明
├─ MCP: 服务端运行 + 可能 Docker 部署
└─ 都需要: 单文件/少文件，启动快
```

### 3.2 无法绕过的问题

#### 问题 1: 无法添加 shebang
```typescript
// tsup 不支持 shebang 注入
// 需要 hack：
"scripts": {
  "build": "tsup && echo '#!/usr/bin/env node' | cat - dist/bin.js > temp && mv temp dist/bin.js"
}
```

#### 问题 2: 代码分割不可控
```typescript
// tsup 会自动代码分割
// 对于 CLI，这反而增加启动延迟（需要加载多个文件）

// 尝试禁用：
splitting: false,  // 只影响库，对多入口应用无效
```

#### 问题 3: ESM 兼容性陷阱
```typescript
// tsc 编译: 保持源代码结构，Node 原生 ESM 处理
tsc --outDir dist

// tsup 打包: 所有代码合并到单个/少量文件
// 问题：
// 1. import.meta.url 行为变化
// 2. __dirname/__filename 消失
// 3. 动态 import 路径可能失效
```

---

## 4. 替代方案对比

### 4.1 方案对比表

| 方案 | 适合场景 | 构建速度 | 配置复杂度 | 产物控制 | 推荐指数 |
|------|----------|----------|------------|----------|----------|
| **保持 tsup** | 现状 | ⭐⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐ | 🟡 不推荐 |
| **tsc 直接编译** | 纯 Node 应用 | ⭐⭐⭐ | ⭐ | ⭐⭐⭐⭐ | 🟢 推荐 |
| **Rollup** | 库 + 应用 | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 🟢 推荐 |
| **Vite (lib模式)** | 需要 HMR 的场景 | ⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐ | 🟡 可选 |
| **pkg/nexe** | 独立可执行文件 | ⭐⭐ | ⭐ | ⭐⭐⭐⭐⭐ | 🟡 特定场景 |

### 4.2 推荐方案：分层构建

```typescript
// 库 (shared/core/acp) → 保持 tsup
// 理由：
// - 类型声明必需
// - 被其他包依赖，需要标准格式
// - 构建速度快

// 应用 (api/cli/mcp-server) → 迁移到 tsc
// 理由：
// - 原生 Node ESM，无打包开销
// - 文件结构清晰，便于调试
// - 支持 shebang（CLI）
// - Docker 构建友好
```

---

## 5. 迁移方案详细设计

### 5.1 目标配置

#### shared/core (保持 tsup)
```typescript
// tsup.config.ts
import { defineConfig } from "tsup";

export default defineConfig({
  entry: ["src/index.ts"],
  format: ["esm"],
  dts: true,
  clean: true,
  sourcemap: true,
});
```

#### api/mcp-server (迁移到 tsc)
```json
// tsconfig.build.json
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./dist",
    "declaration": false,      // 服务不需要类型声明
    "sourceMap": false,        // 生产可关闭
    "composite": false
  },
  "include": ["src/**/*"],
  "exclude": ["src/**/*.test.ts"]
}
```

```json
// package.json
{
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx --watch src/index.ts",
    "start": "node dist/index.js"
  }
}
```

#### cli (迁移到 tsc + shebang)
```json
// tsconfig.build.json
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./dist",
    "declaration": false
  }
}
```

```json
// package.json
{
  "scripts": {
    "build": "tsc -p tsconfig.build.json && npm run fix-shebang",
    "fix-shebang": "echo '#!/usr/bin/env node' | cat - dist/bin/agentcraft.js > temp.js && mv temp.js dist/bin/agentcraft.js && chmod +x dist/bin/agentcraft.js"
  },
  "bin": {
    "agentcraft": "./dist/bin/agentcraft.js"
  }
}
```

### 5.2 迁移成本评估

| 包 | 工作量 | 风险 | 优先级 |
|----|--------|------|--------|
| api | 2 小时 | 低 | P1 |
| mcp-server | 2 小时 | 低 | P1 |
| cli | 4 小时 | 中（shebang 处理） | P1 |
| shared | 0 | 无 | 保持 |
| core | 0 | 无 | 保持 |

**总工作量**: 约 1 人天

### 5.3 验证清单

```bash
# 构建测试
npm run build

# CLI 测试
./packages/cli/dist/bin/agentcraft.js --version  # 应可直接执行

# API 测试
node packages/api/dist/index.js  # 应能正常启动

# Docker 构建测试
docker build -t agentcraft-api packages/api
```

---

## 6. 不迁移的风险

### 6.1 短期风险（1-3 个月）

| 风险 | 影响 | 概率 |
|------|------|------|
| CLI 发布到 npm 后用户无法直接运行 | 高 | 高 |
| Docker 镜像体积过大 | 中 | 中 |
| 调试困难（sourcemap 与实际文件不对应） | 中 | 中 |

### 6.2 长期风险（3-6 个月）

| 风险 | 影响 | 概率 |
|------|------|------|
| ESM 兼容性问题暴露（__dirname 等） | 高 | 中 |
| 构建产物难以优化 | 中 | 高 |
| 团队需要维护 tsup 的 workarounds | 中 | 高 |

---

## 7. 结论与建议

### 7.1 核心结论

```
┌─────────────────────────────────────────────────────────┐
│  tsup 是优秀的库构建工具，但不是万能工具                  │
│  AgentCraft 的混合形态（库+应用）需要分层构建策略        │
└─────────────────────────────────────────────────────────┘
```

### 7.2 行动建议

#### 立即执行（本周）
1. **cli 包添加 shebang 后处理脚本**
   ```json
   "build": "tsup && node scripts/add-shebang.js"
   ```
   缓解无法直接运行的问题。

#### 短期执行（本月）
2. **api/cli/mcp-server 迁移到 tsc**
   - 预期收益: 产物优化，Docker 友好
   - 工作量: 1 人天
   - 风险: 低

3. **添加构建产物验证 CI 步骤**
   ```yaml
   - name: Verify CLI executable
     run: ./packages/cli/dist/bin/agentcraft.js --version
   ```

#### 中期考虑（3 个月内）
4. **评估引入 Turborepo**
   - 统一构建管道
   - 优化 monorepo 构建顺序

### 7.3 最终评估

| 维度 | 评分 | 说明 |
|------|------|------|
| 当前适用性 | 6/10 | 能用，但有明显局限 |
| 长期适用性 | 4/10 | 不适合应用包的增长 |
| 迁移收益 | 8/10 | 显著改善构建产物 |
| 迁移成本 | 3/10 | 工作量小，风险低 |

**综合建议**: 🟢 **建议迁移**，投入产出比高。

---

*报告生成时间: 2026-02-20*
*分析范围: 所有 packages 的构建配置和产物*
